---
title: 'Atualizar deal'
openapi: 'PATCH /deals/{id}'
---

<Note>
  Para mover de estagio ou alterar status, use os endpoints especificos `PATCH /deals/{id}/stage` e `PATCH /deals/{id}/status` — eles registram historico automaticamente.
</Note>
