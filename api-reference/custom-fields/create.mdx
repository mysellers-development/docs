---
title: 'Criar campo personalizado'
openapi: 'POST /custom-fields'
---

<Tip>
  O `fieldName` e o slug usado como chave no `customValues` do lead. Use apenas letras minusculas, numeros e underscore (ex: `cargo`, `setor_empresa`).
</Tip>
