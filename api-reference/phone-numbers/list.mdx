---
title: 'Listar telefones'
openapi: 'GET /phone-numbers'
---

<Tip>
  Endpoint flexivel: aceita filtros por `lead_id`, `user_id`, `workspace_id` ou `number`. Quando autenticado via **API Key**, o `workspace_id` e inferido do token automaticamente.
</Tip>
