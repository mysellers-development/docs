---
title: 'Marcar mensagens como lidas'
openapi: 'PATCH /unified-messages/chat/{chatId}/read'
---

<Note>
  Zera o `unreadCount` do chat e atualiza o status das mensagens inbound nao lidas para `read`.
</Note>
