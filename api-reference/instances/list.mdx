---
title: 'Listar instancias'
openapi: 'GET /instances'
---

<Tip>
  Aceita filtros `workspace_id` e `name`. Quando autenticado via **API Key**, o workspace e inferido automaticamente do token.
</Tip>
